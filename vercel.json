{
  "version": 2,
  "buildCommand": "pnpm --filter @workspace/print-sahyogi build",
  "outputDirectory": "build",
  "installCommand": "pnpm install --no-frozen-lockfile",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
